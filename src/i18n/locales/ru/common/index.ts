import commons from './commons.json';
import validators from './validators.json';
import travelForm from './travel-form.json';

export const common = {
    commons,
    validators,
    'travel-form': travelForm,
};

export default common;
